% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accu.R
\name{cer_accu_projects}
\alias{cer_accu_projects}
\title{ACCU Scheme project register}
\source{
Clean Energy Regulator, ACCU project and contract register:
\url{https://cer.gov.au/markets/reports-and-data/accu-project-and-contract-register}.
Licensed under CC BY 4.0.
}
\usage{
cer_accu_projects(
  state = NULL,
  method = NULL,
  status = c("active", "revoked", "completed", "all")
)
}
\arguments{
\item{state}{Optional character vector of Australian state
codes (e.g. \code{"NSW"}, \code{c("VIC", "QLD")}) to filter on. \code{NULL}
returns projects across all states.}

\item{method}{Optional character vector of method name
substrings (case-insensitive, e.g. \code{"savanna"}, \code{"landfill"},
\code{"vegetation"}).}

\item{status}{Character. One of \code{"active"} (default), \code{"revoked"},
\code{"completed"}, or \code{"all"}.}
}
\value{
A \code{cer_tbl} (data frame) with one row per ACCU project.
}
\description{
Download and parse the Clean Energy Regulator's Australian
Carbon Credit Unit ('ACCU') Scheme project register. Returns one
row per project with proponent, method, state, crediting period,
permanence period, and cumulative ACCUs issued and relinquished.
}
\details{
The register is updated monthly by the CER, usually around the
third week. Downloaded once per R session (or invalidate with
\code{\link[=cer_clear_cache]{cer_clear_cache()}}).
}
\examples{
\donttest{
op <- options(cer.cache_dir = tempdir())
p <- cer_accu_projects(state = "NSW")
head(p)
options(op)
}
}
\seealso{
Other accu: 
\code{\link{cer_accu_contracts}()},
\code{\link{cer_accu_issuances}()},
\code{\link{cer_accu_methods}()},
\code{\link{cer_accu_relinquishments}()}
}
\concept{accu}

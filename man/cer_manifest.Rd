% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{cer_manifest}
\alias{cer_manifest}
\title{Return the session manifest of fetched CER datasets}
\usage{
cer_manifest(format = c("df", "yaml", "json"))
}
\arguments{
\item{format}{One of \code{"df"} (default), \code{"yaml"}, or \code{"json"}.}
}
\value{
A data frame, YAML string, or JSON string.
}
\description{
Every call to a data function appends one row to the session
manifest, recording URL, dataset title, SHA-256 of the cached
file, size, retrieval timestamp, and the snapshot pin set via
\code{\link[=cer_snapshot]{cer_snapshot()}}. Duplicate URLs within a session are
deduplicated (last fetch wins).
}
\details{
For carbon market research this is the minimum artefact needed
for reproducibility: ACCU project registers and Safeguard
releases change retroactively, so a citation that points to a
URL alone is not enough. The manifest plus SHA-256 fixes the
exact bytes analysed.
}
\examples{
\donttest{
op <- options(cer.cache_dir = tempdir())
cer_manifest_clear()
cer_snapshot("2026-04-24")
cer_manifest()
options(op)
}
}
\seealso{
Other reproducibility: 
\code{\link{cer_cite}()},
\code{\link{cer_deposit_zenodo}()},
\code{\link{cer_legislative_instrument}()},
\code{\link{cer_manifest_clear}()},
\code{\link{cer_manifest_write}()},
\code{\link{cer_sha256}()},
\code{\link{cer_snapshot}()}
}
\concept{reproducibility}

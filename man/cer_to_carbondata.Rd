% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{cer_to_carbondata}
\alias{cer_to_carbondata}
\title{Remap an ACCU project data frame to the carbondata voluntary-
project schema}
\usage{
cer_to_carbondata(x)
}
\arguments{
\item{x}{A \code{cer_tbl} from \code{\link[=cer_accu_projects]{cer_accu_projects()}}.}
}
\value{
A data frame with carbondata-style columns:
\code{project_id}, \code{project_name}, \code{methodology}, \code{project_type},
\code{country}, \code{registry}, \code{issued_credits_to_date},
\code{retired_credits_to_date}.
}
\description{
The \code{carbondata} R package exposes Verra, Gold Standard, ACR,
and CAR voluntary projects under a unified schema. This function
renames ACCU register columns to match, so an analyst can
compare ACCU issuance patterns against international voluntary
markets on a consistent basis.
}
\details{
Schema mapping is lossy: ACCU-specific fields (Chubb flag,
permanence period, crediting-period dates) pass through with
original names.
}
\examples{
\donttest{
op <- options(cer.cache_dir = tempdir())
try({
  ccy <- cer_accu_projects()
  carb <- cer_to_carbondata(ccy)
  head(carb)
})
options(op)
}
}
\seealso{
Other interop: 
\code{\link{cer_international_comparator}()}
}
\concept{interop}

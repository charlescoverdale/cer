% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accu_integrity.R
\name{cer_accu_aggregate}
\alias{cer_accu_aggregate}
\title{Integrity-weighted ACCU aggregation}
\usage{
cer_accu_aggregate(x, value_col = "accus_issued", warn_contested_pct = 0.05)
}
\arguments{
\item{x}{A \code{cer_tbl} or data frame with columns \code{method} and
(typically) \code{accus_issued}.}

\item{value_col}{Column to sum. Default \code{"accus_issued"}.}

\item{warn_contested_pct}{Warn if the contested tier exceeds
this share of the total. Default \code{0.05} (5\%).}
}
\value{
A data frame with one row per integrity tier and
columns \code{integrity_tier}, \code{projects}, \code{accus_issued_sum},
\code{share_pct}.
}
\description{
Takes an \code{ato_tbl} or data frame of ACCU project records (from
\code{\link[=cer_accu_projects]{cer_accu_projects()}}), joins the method integrity scorecard,
and aggregates by integrity tier. Emits a warning if contested
or under-review methods contribute materially to the total so
the analyst cannot silently aggregate them into a single
headline figure.
}
\examples{
\donttest{
op <- options(cer.cache_dir = tempdir())
try({
  p <- cer_accu_projects()
  cer_accu_aggregate(p)
})
options(op)
}
}
\seealso{
Other accu: 
\code{\link{cer_accu_contracts}()},
\code{\link{cer_accu_issuances}()},
\code{\link{cer_accu_methods}()},
\code{\link{cer_accu_projects}()},
\code{\link{cer_accu_relinquishments}()},
\code{\link{cer_method_determination}()},
\code{\link{cer_method_integrity}()}
}
\concept{accu}
